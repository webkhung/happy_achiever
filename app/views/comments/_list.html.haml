.comments
  - commentable.comments.recent.limit(10).all.each do |c|
    %p #{link_to(c.user.display_name, c.user)}  #{c.comment} <span class='datetime'>#{c.created_at.strftime('%D')}</span>

  = simple_form_for [commentable, Comment.new] do |f|
    .input-append
      = f.text_field :comment
      = f.submit 'Submit', class: 'btn'
      = hidden_field_tag :back, params[:back]
