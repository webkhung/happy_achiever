- if @user.plans.present?
  .alert.alert-info
    %i
      %small We don't need to wait to be happy after we achieved our goals. We can feel happy while we are working on the goal as long as we are growing, learning and enjoy the process.

  %h2
    You have #{pluralize(current_user.plans.count, 'Goal')}
    %small Take massive action and enjoy the journey!

  .row
    .span8.plan-overview
      - @user.plans.each do |plan|
        .row
          .span3
            %p= link_to image_tag(plan.image_path, class: 'img-polaroid plan-large'), plan_path(plan) if plan.image_path.present?
          .span5
            %h3
              %strong= plan.title.titleize

            %ul.unstyled
              %li= plan_field_with_icon(:eye_open, 'Vision', plan.vision)
              %li= plan_field_with_icon(:question, 'Why', plan.purpose)
              %li= plan_field_with_icon(:frown, 'Fail to follow through', plan.if_not_achieved)
              %li= "#{icon_for(:time, 'Time scheduled')} #{time_spent_in_words(plan.minutes_scheduled)} this week".html_safe
              %li= "#{icon_for(:time, 'Time spent')} #{time_spent_in_words(plan.time_spent_last_week)} this week".html_safe

            - if plan.focus_areas.empty?
              %p
                = link_to 'Add focus area', new_plan_focus_area_path(plan), class: 'btn btn-danger btn-mini'
                %span.label It is important to add focus area! Please do so now!
            - elsif plan.tasks.empty?
              %p
                = link_to 'Add tasks', plan_path(plan), class: 'btn btn-danger btn-mini'
                %span.label It is important to add tasks to focus areas! Please do so now!

            -# This gives the scheudle of a given plan
            -#= render 'schedules/today_tomorrow', plan: plan

            - if (last_achievement = plan.last_achievement) && last_achievement.achieved_date < 1.week.ago
              %p.text-center
                = link_to("Attention!", "#modal-plan#{plan.id}", { data: { toggle: "modal" }, class: 'btn btn-danger'} )
              %p
                %small.attention You have done nothing for one week on this goal. Click on the button now.

            = render 'plans/attention_modal', plan: plan

            -#- if plan.tasks.present?
            -#  %p
            -#    = icon_for(:calendar)
            -#    = link_to 'Add Schedule', schedules_path, class: 'btn btn-success btn-mini'
          .clear-both
          %br
      #no-plan-container= link_to 'Click here to add a new goal', new_plan_path, id: 'add-goal'
    .span4
      = render partial: 'schedules/day_range', :locals => { start_date: DateTime.now.to_date, days: 2, schedules: current_user.schedules.all, mode: :view }
- else
  %h2 You have not set up a goal yet
  #no-plan-container= link_to 'Click here to add a new goal', new_plan_path, id: 'add-goal'
