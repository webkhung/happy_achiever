- if (undone_tasks = plan.undone_tasks).present?
  .status{ style: "background-image: url('/assets/attention.png')"}
    Tips: schedule and take action on the following #{pluralize(undone_tasks.count, 'task')}
    = undone_tasks.map{ |t| content_tag(:strong, t.description) }.to_sentence.html_safe
    .quote &#8220;A journey of a thousand miles begins with a single step.&#8221; - Lao-tzu
