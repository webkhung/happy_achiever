- title "New Empowerment"

- if @achievement.state_id == Achievement::UNENERGIZED
  = render 'energize'
- elsif @achievement.state_id == Achievement::OVERWHELMED
  = render 'overwhelmed'
- else
  = render 'form'
