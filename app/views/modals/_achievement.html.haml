#achievement_modal.modal.hide.fade
  .modal-header
    %button.close{ type: 'button', data: { dismiss: 'modal' } } &times;
    - if current_user.achievements.count == 1
      %h2= icon_for(:trophy, 'Thank You For Recording Your State!')
    - else
      %h1= icon_for(:trophy, 'Great Job!')

  .modal-body
    - if current_user.achievements.count == 1
      .well
        %p
          = image_tag 'Happy.png'
          If you are happy, write it down so you can repeat in the future.
        %p
          = image_tag 'Sad.png'
          If you are unhappy, this tool could help you understand the reason and make you feel better.
      %p Recording your state everyday is like <span class='orange'>Building Your Emotional Muscle.</span>
      %p= image_tag 'brain-exercising.gif', align: 'right'
      %p Doing it everyday will  <span class='orange'>Make You Strong!</strong>
      %p This is your first <span class='orange'>ACHIEVEMENT</span> as a Happy Achiever!
      .text-center= image_tag 'star.png', :size => "60x60"
    - elsif current_user.achievements.count > 1
      .text-center
        %h1.orange
          %strong= "#{pluralize(current_user.achievements.count, 'Achievement')}!"
        %p= image_tag 'clapping.jpg'
      - if current_user.achievements.last.is_state?
        %p Recording your state everyday is like <span class='orange'>Building Your Emotional Muscle.</span>
        %p= image_tag 'brain-exercising.gif', align: 'right'
        %p Doing it everyday will  <span class='orange'>Make You Strong!</strong>
      - else
        %h3.text-center Whenever you took an action:
        %p
          = image_tag 'seedlings-growing.jpg', width: '200x100', align: 'right'
          You have <strong>GROWTH</strong><br>
          You have <strong>LEARNED</strong> something new<br>
          You are <strong>ONE STEP CLOSER</strong> to your goal<br>

  .modal-footer
    = link_to 'Continue!', '#', data: { dismiss: 'modal' }

:javascript
  window.onLoadModal = '#achievement_modal';
